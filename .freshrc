# handles updating fresh
fresh freshshell/fresh bin/fresh --bin

# adds rails, rspec and zeus support
fresh jasoncodes/dotfiles shell/aliases/rails.sh

# open current github project
fresh twe4ked/dotfiles bin/gh --bin

fresh shell/\*
fresh shell/aliases/\*
fresh shell/config/\*
fresh bin/\*
