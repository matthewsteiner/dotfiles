# handles updating fresh
fresh freshshell/fresh bin/fresh --bin

fresh shell/\*
fresh shell/aliases/\*
fresh shell/config/\*
fresh config/\* --file

fresh bin/browse-repository --bin=~/bin/browse-repository
